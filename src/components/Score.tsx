import React, { Fragment, useState } from 'react'

export default function Score() {
    // const { difficulty, count } = JSON.parse(localStorage.getItem("score"))
    // const [score, setDice] = useState();

    // const scoreList: JSX.Element[] = score.map(el =>
    //     <h2>{difficulty} | {count}</h2>)

    // return (
    //     <Fragment>
    //         <h2>difficulty | count</h2>
    //         {scoreList}
    //     </Fragment>

    // )
}
